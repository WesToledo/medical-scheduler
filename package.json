{
  "scripts": {
    "dev": "dotenv -- turbo dev",
    "build": "turbo run build",
    "start:prod": "node apps/backend/dist/main"
  },
  "devDependencies": {
    "dotenv-cli": "^7.3.0",
    "turbo": "^1.10.15"
  },
  "workspaces": [
    "apps/*"
  ]
}
